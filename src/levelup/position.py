import logging
from dataclasses import dataclass
from enum import Enum

class Position(int):
    x_position: int = 5
    y_position: int = 5

def getCurrentPosition():
    print("My Current Position: " + this.x_position + "," + this.y_position)


